import React from 'react';

// CreateOrgModal intentionally removed — keep a harmless stub to avoid import errors during cleanup.
const CreateOrgModal: React.FC<{ open?: boolean; onClose?: () => void }> = () => null;

export default CreateOrgModal;
